#include "main.h"
/**
*mul - multiplies the input parameters.
*@a: paramter to be multiplied to b.
*@b: paramter to be multiplied to a.
*Return: the value a*b.
*/
int mul(int a, int b)
{
return (a*b);
}
